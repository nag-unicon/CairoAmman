    SUBROUTINE CAB.ACCOUNT.PARAM.RTN(CATEG.STMT,AUT.VAL,STMT.ENT.ID,ENTRY.REC)
    $INCLUDE T24.BP I_COMMON
    $INCLUDE T24.BP I_EQUATE
    $INCLUDE T24.BP I_ACCT.COMMON


    CALL VIVEO.TRANS.FILTER.ACCT.RTN(CATEG.STMT,AUT.VAL,STMT.ENT.ID,ENTRY.REC)

    CALL CAB.AUTH.UPD.FT.TXNS.CALL

    IF RUNNING.UNDER.BATCH THEN
        CALL CAB.S2M.STO.MSG(ENTRY.REC)
    END

    RETURN
