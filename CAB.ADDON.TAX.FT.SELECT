    SUBROUTINE CAB.ADDON.TAX.FT.SELECT

    $INSERT T24.BP I_COMMON
    $INSERT T24.BP I_EQUATE
    $INSERT CAB.BP I_CAB.ADDON.TAX.CALC.POST.FT

    Y.SEL.CMD = '' ; Y.SEL.LIST = ''
    Y.SEL.CMD = 'SELECT ':FN.INT.ACCUM:' LIKE ...-':Y.END.DATE
    CALL EB.READLIST(Y.SEL.CMD,Y.SEL.LIST,'','','')

    IF Y.SEL.LIST NE '' THEN
        CALL BATCH.BUILD.LIST("",Y.SEL.LIST)
    END

    RETURN

END
